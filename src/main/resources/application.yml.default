spring:
  datasource:
    url: jdbc:mysql://localhost/ec?useSSL=false
    username:
    password:
    driverClassName: com.mysql.jdbc.Driver

mybatis:
  configuration:
    mapUnderscoreToCamelCase: true